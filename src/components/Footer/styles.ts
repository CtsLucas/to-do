import styled from "styled-components"

export const Container = styled.footer`
  position: absolute;
  bottom: 0;
  margin-bottom: 1rem;

  width: 100%;
  text-align: center;
  font-size: 0.75rem;

  span {
    font-weight: 700;
  }
`
